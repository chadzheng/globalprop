'use strict';

/**
 * @ngdoc function
 * @name nodejsApp.controller:MainCtrl
 * @description
 * # Search
 * Controller of the nodejsApp
 */
angular.module('nodejsApp')
  .controller('headerCtrl', function ($rootScope,$scope,district,base,news,$modal,$filter,$location) {

    $scope.responsiveMeta={
        0:'',
        1:'<meta name="viewport" content="width=device-width, initial-scale=1">'
    }

    
  });
